<div class="topnav">
  <% if current_user %>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
  <% end %>
  <%= link_to "UsersIndex", users_path, method: :get%>
</div>

<h1>Users!</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
